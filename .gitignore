*.iml
.gradle
/local.properties
.DS_Store
/build
/captures
.externalNativeBuild
.cxx

# IDE. The whole directory goes; the per file rules it replaces were listing a
# handful of files inside a directory that was already ignored below them.
.idea/

# Kotlin build session data, created at the project root by the compiler
.kotlin/

# Release signing
/keystore/keystore.properties
/keystore/*.jks
