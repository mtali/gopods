# Copy to keystore.properties and fill in to sign release builds with the upload key.
# Without this file the release build falls back to the debug key so it still assembles.
storeFile=keystore/upload.jks
storePassword=
keyAlias=
keyPassword=
